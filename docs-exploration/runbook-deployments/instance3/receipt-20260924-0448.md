PLANNED

## Target Instance & Resources
- **Instance**: `instance3`
- **Resource Prefix**: `ax-instance3`
- **GCP Project**: `barni-cnrm-20260529`
- **Region / Zone**: `us-central1` / `us-central1-a`
- **GKE Cluster**: `ax-instance3-gke` (3 nodes, `e2-standard-4`, Workload Identity enabled, label: `repo-agent-instance=ax-instance3`)
- **GCS Snapshots Bucket**: `gs://ax-instance3-snapshots` (location: `us-central1`, label: `repo-agent-instance=ax-instance3`)
- **GCP Service Account**: `ax-instance3-sa@barni-cnrm-20260529.iam.gserviceaccount.com`
- **Container Image Repositories**:
  - `gcr.io/barni-cnrm-20260529/ax-instance3-images/ax-task-runner`
  - `gcr.io/barni-cnrm-20260529/ax-instance3-images/substrate`
  - `gcr.io/barni-cnrm-20260529/ax-instance3-images` (for `ax-controller` and `ax-server` via `ko`)

## Estimated Cost
- **Runtime Cost**: Approximately **$0.01 - $0.05 per hour** (primarily 3x `e2-standard-4` GKE worker node VMs and storage usage).
- **Teardown Cost**: Negligible when torn down via `teardown.sh` immediately after validation.

## Feasibility State
- [x] **gcloud CLI**: ✓ Present (`gcloud version` active, authenticated as `cnrm-barni-1.svc.id.goog`, project: `barni-cnrm-20260529`)
- [x] **kubectl CLI**: ✓ Present (`v1.35.8-dispatcher`)
- [x] **Go 1.27+ Compiler**: ✓ Present (`go1.27.1`)
- [ ] **ko CLI**: ✗ MISSING (Required on operator machine to build/push Go containers for Substrate and AX via `go install github.com/google/ko@latest`)
- [ ] **Docker / Podman Daemon**: ✗ MISSING (Required locally to build and push `ax-task-runner` container image)
- [ ] **Agent Substrate Source Code**: ✗ Cloned dynamically in `deploy.sh` (`git clone https://github.com/agent-substrate/substrate.git`)

## Deviations from the Runbook
- None. All script steps follow `docs-exploration/runbooks/deploy-gcp.md` directly.

## Notes for Owner Before Deploying
1. Ensure `ko` CLI is installed and in `$PATH` or available in the deployment environment.
2. Ensure Docker or Podman daemon is running to allow `make push-task-runner` to build the linux/amd64 task runner container image.
3. Verify IAM permissions for `cnrm-barni-1.svc.id.goog` on project `barni-cnrm-20260529` include `roles/container.admin`, `roles/storage.admin`, `roles/iam.admin`, and `roles/artifactregistry.admin` (or storage access for GCR).
