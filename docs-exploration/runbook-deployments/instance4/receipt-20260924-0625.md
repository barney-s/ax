PLANNED

## Instance Information
- **Instance**: instance4
- **Runbook**: `docs-exploration/runbooks/deploy-gcp-md.md`
- **Resource Prefix**: `ax-instance4`
- **GCP Project**: `barni-cnrm-20260529` (Project Number: `77658989016`)
- **GCP Region / Zone**: `us-central1` / `us-central1-a`

## Resources to be Created
- **GKE Cluster**: `ax-instance4` (Zone: `us-central1-a`, Machine Type: `e2-standard-4`, Node Pool: `substrate-node-pool`)
- **GCS Snapshot Bucket**: `gs://ax-instance4-snap-77658989016/`
- **GCP Service Account**: `ax-instance4-sa@barni-cnrm-20260529.iam.gserviceaccount.com`
- **Container Repositories & Images**:
  - Substrate Components: `gcr.io/barni-cnrm-20260529/ax-instance4/*`
  - AX Task Runner: `gcr.io/barni-cnrm-20260529/ax-instance4-images/ax-task-runner:latest`
  - AX Controller & Server: `gcr.io/barni-cnrm-20260529/ax-instance4-images/*`
- **Kubernetes Workloads & Namespaces**:
  - Namespace `ate-system`: Agent Substrate control plane (`substrate-controller`, `ate-api-server`, `atelet`, CRDs, routing gateway)
  - Namespace `ax-system`: AX Redis (`ax-redis`), AX API Server (`ax-server`), AX Controller (`ax-controller`)

## Estimated Cost
- Approximately **$0.01 - $0.05 per hour** of runtime (primarily standard GKE `e2-standard-4` compute instances and minimal GCS storage).
- Complete cleanup via `teardown.sh` returns ongoing cost to $0.

## Feasibility Checklist
- [x] **gcloud CLI**: ✓ Present (Google Cloud SDK 586.0.0, Project `barni-cnrm-20260529`, Account `cnrm-barni-1.svc.id.goog`)
- [x] **kubectl CLI**: ✓ Present (v1.35.8)
- [x] **Go Compiler**: ✓ Present (go1.27.1 linux/amd64)
- [ ] **ko CLI**: ✗ MISSING (Install via `go install github.com/google/ko@latest`)
- [ ] **Docker / Podman Daemon**: ✗ MISSING (Install Docker/Podman and start daemon for container builds)
- [ ] **Agent Substrate Source Code**: ✗ MISSING (Cloned automatically by `deploy.sh` from `https://github.com/agent-substrate/substrate.git`)

## Needs from Owner
- The runbook specifies building container images with local Docker/Podman and `ko`. Because neither `ko` nor a container runtime daemon is currently installed in this environment, they must be present when executing `deploy.sh`, or the runbook should be updated to leverage Cloud Build / remote image builders.

## Important Notes in deploy.sh
- All resources are prefixed with `ax-instance4` to guarantee isolation and ownership.
- Workload Identity binds Kubernetes Service Accounts (`ax-system/ax-controller`, `ate-system/substrate-controller`, `ate-system/atelet`) to `ax-instance4-sa@barni-cnrm-20260529.iam.gserviceaccount.com`.
- GKE node pool auto-upgrade is disabled on `substrate-node-pool` to prevent disruption during execution.
