VERIFIED

## Attempt Log

### Attempt 1
- **What Failed:** Step 3 (`hack/install-ate.sh`) failed because `CLUSTER_LOCATION` was not set in the environment or inside the `.ate-dev-env.sh` config.
- **Evidence:** `Error: PROJECT_ID is set but CLUSTER_NAME and CLUSTER_LOCATION are not; set them in .ate-dev-env.sh or pass --context to use an already-configured cluster`
- **What was changed:** Added `export CLUSTER_LOCATION="${ZONE}"` to `params.env` and included it in the `.ate-dev-env.sh` generator in `deploy.sh`. Added an explicit `gcloud container clusters get-credentials` command to authenticate `kubectl` in `deploy.sh`. Added `/workspaces/.home/go/bin` to `PATH` in `deploy.sh` to ensure `ko` could be found.

### Attempt 2
- **What Failed:** Step 2 (`setup-gcp bootstrap`) failed because the GCE Region parameter was not specified, causing it to default to `us-west1` which is incompatible with the zone `us-central1-a`.
- **Evidence:** `Error: cluster location "us-central1-a" is not compatible with region "us-west1": cluster location must be the region itself (for regional clusters) or a zone within that region (e.g. us-west1-c)`
- **What was changed:** Added `export GCE_REGION="${REGION}"` to `params.env` and included it in the `.ate-dev-env.sh` generator in `deploy.sh`.

### Attempt 3
- **What Failed:** 
  1. GCS Bucket region mismatch: The pre-existing bucket `ate-snapshots-barni-cnrm-20260529` was created in region `US-WEST1`, which caused a location mismatch during us-central1 bootstrap.
  2. Google Cloud Build failure: Cloud Build ignores the `bin/` directory by default since it is in `.gitignore`, causing `ax-task-runner` binary COPY to fail.
- **Evidence:** 
  1. `Error: bucket ate-snapshots-barni-cnrm-20260529 is in location US-WEST1, but expected us-central1`
  2. `COPY failed: file not found in build context: stat bin/linux_amd64/ax-task-runner: file does not exist`
- **What was changed:**
  1. Appended `-us-central1` suffix to bucket name (`ate-snapshots-${PROJECT_ID}-us-central1`) to create a fresh bucket in the correct region, bypassing GCS soft-delete name retention and location mismatch.
  2. Created a `custom-gcloudignore` file to ensure the compiled `bin/linux_amd64/ax-task-runner` is included in the GCB uploaded build context.
  3. Discovered and resolved a Substrate worker scheduling error by manually compiling/publishing `ateom-gvisor` worker images (`ate-setup publish worker-images` inside Substrate repo) and provisioning a `WorkerPool` in the `default` namespace.
  4. Discovered and resolved actor suspension 403 Forbidden crash by adding the `AX_SNAPSHOTS_BUCKET` environment variable with a placeholder to `deploy/ax-controller.yaml` and replacing it dynamically with `${BUCKET_NAME}` in `deploy.sh`.
  5. Implemented dynamic digest resolution in `demo.sh` to pin the `ax-task-runner` image via its `sha256` digest, satisfying Substrate validation.

## Verify Evidence

Executing `./demo.sh` completed successfully with the following highlights:
1. **Task Provisioning:** `demo-task` successfully created and reached `Running` status in 15 seconds.
2. **Connectivity:** `ax ssh` executed file listings (`ls -la /workspace/chalk`) and curled the Metadata HTTP API inside the task:
   ```
   $ ax ssh demo-task -- sh -c 'echo "DEMO_ENV=$DEMO_ENV"; echo "AX_METADATA_URL=$AX_METADATA_URL"'
   DEMO_ENV=active
   AX_METADATA_URL=http://127.0.0.1:80
   ```
3. **Suspension:** `ax suspend task` completed successfully in 3 seconds, checkpointed state, and tore down the sandbox with no GCS permission errors.

## Teardown Contract & Running Resources

The following GCP resources are left running:
- **GKE Cluster:** `ics-1` in zone `us-central1-a` (2 nodes, machine type `c3-standard-4`) running Agent Substrate control plane (`ate-system`), WorkerPool daemonsets, and AX components (`ax-system`, including `ax-redis`, `ax-controller`, and `ax-server`).
- **GCS Bucket:** `ate-snapshots-barni-cnrm-20260529-us-central1` in `us-central1` (used for task snapshots).
- **GCR Images:** Built and uploaded under `gcr.io/barni-cnrm-20260529/ate-images/`:
  - `ax-task-runner`
  - `ax-controller`
  - `ax-server`
  - Substrate images (`ateapi`, `atecontroller`, `atelet`, `atenet`, `ateom-gvisor`, `podcertcontroller`)

To clean up all resources, execute:
```bash
./docs-exploration/runbook-deployments/ak-ate-1/teardown.sh
```
