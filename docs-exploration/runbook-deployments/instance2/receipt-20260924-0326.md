PLANNED

# Deployment Instance Plan Receipt

- **Planned Date (UTC):** 2026-09-24 03:26
- **Target Instance:** `instance2`
- **Target Directory:** `docs-exploration/runbook-deployments/instance2/`

---

## What this instance will create

Derived directly from `params.env`:
1. **Google Kubernetes Engine (GKE) Cluster**:
   - Name: `ax-instance2-gke`
   - Setup: Standard zonal cluster in zone `us-central1-a` with 3 nodes of type `e2-standard-4`, configured with GcePersistentDiskCsiDriver and Workload Identity enabled.
   - Resource Labels: `repo-agent-instance=ax-instance2`
2. **Google Cloud Storage (GCS) Snapshot Bucket**:
   - Name: `gs://ax-instance2-snapshots`
   - Location: `us-central1`
   - Resource Labels: `repo-agent-instance=ax-instance2`
3. **Google Cloud Service Account (GSA)**:
   - Name: `ax-instance2-sa`
   - Email: `ax-instance2-sa@barni-cnrm-20260529.iam.gserviceaccount.com`
   - Role: `roles/storage.objectAdmin` on `gs://ax-instance2-snapshots`, `roles/storage.objectViewer` and `roles/artifactregistry.reader` on project `barni-cnrm-20260529`
4. **Workload Identity Role Bindings**:
   - `ate-system/substrate-controller` -> `ax-instance2-sa`
   - `ate-system/atelet` -> `ax-instance2-sa`
   - `ax-system/ax-controller` -> `ax-instance2-sa`
   - `ax-system/default` -> `ax-instance2-sa`
   - `default/default` -> `ax-instance2-sa`
5. **Kubernetes Namespaces & Service Accounts**:
   - Namespaces `ate-system` and `ax-system`
   - ServiceAccount `ax-controller` (annotated with GSA email)
   - ServiceAccount `default` in `ax-system` and `default` namespaces (annotated with GSA email)
6. **Agent Substrate Control Plane**:
   - Custom Resource Definitions (`substrate-crds` chart version 0.0.12)
   - Core Substrate platform components (`substrate` chart version 0.0.12)
7. **Container Images**:
   - Repository: `gcr.io/barni-cnrm-20260529/ax-instance2-images`
   - Guest Supervisor: `gcr.io/barni-cnrm-20260529/ax-instance2-images/ax-task-runner:latest` (built via Google Cloud Build)
8. **AX Application Components**:
   - Redis deployment (`deploy/redis.yaml`)
   - AX API Server and Horizontal Controller workers deployed via `ko`

---

## Cost Estimates

- **GKE Cluster Management Fee**: ~$0.10 per hour.
- **Node Cost (3 x e2-standard-4)**: ~$0.40 per hour.
- **Total Hourly Estimate**: ~**$0.50 per hour** of active cluster runtime.
- **Optimization Strategy**: Executing `teardown.sh` immediately after validation is complete keeps costs minimal.

---

## Feasibility Checklist for this Operator Environment

Based on actual checks performed in the operator environment:
- [x] **gcloud CLI**: ✓ PRESENT (Google Cloud SDK 586.0.0, active project: `barni-cnrm-20260529`)
- [x] **kubectl CLI**: ✓ PRESENT (Client Version: `v1.35.8-dispatcher`)
- [x] **Go 1.27+ Compiler**: ✓ PRESENT (`go version go1.27.1 linux/amd64`)
- [ ] **Helm CLI**: ✗ MISSING (Install via `curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash`)
- [ ] **ko CLI**: ✗ MISSING (Install via `go install github.com/google/ko@latest`)
- [ ] **Docker / Podman Daemon**: ✗ MISSING (Bypassed in `deploy.sh` by submitting `ax-task-runner` build to Google Cloud Build)
- [x] **Agent Substrate OCI Charts**: ✓ READY (Pullable from `oci://ghcr.io/kagent-dev/substrate/helm/`)

---

## Important Notes for the Owner/Operator

Before clicking **Deploy**, please note the following in `deploy.sh`:
1. **Tool Prerequisites**: Ensure Helm and ko are available in the path when `deploy.sh` is executed.
2. **Deterministic & Isolated Ownership**: The deployment is strictly namespaced under resource prefix `ax-instance2`, and all cloud assets carry `repo-agent-instance=ax-instance2`.
3. **No Secrets**: Authentication is managed securely via GKE Workload Identity without any static service account keys.
4. **Automated Teardown**: Running `teardown.sh` deletes all cloud infrastructure (GKE cluster, GCS bucket, container images, and service account) cleanly.
