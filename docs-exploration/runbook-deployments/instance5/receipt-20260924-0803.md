PLANNED

## Instance Information
- **Instance**: instance5
- **Runbook**: `docs-exploration/runbooks/deploy-gcp.md`
- **Resource Prefix**: `ax-instance5`
- **GCP Project**: `barni-cnrm-20260529` (Project Number: `77658989016`)
- **GCP Region / Zone**: `us-central1` / `us-central1-a`

## Resources to be Created
- **GKE Cluster**: `ax-instance5` (Zone: `us-central1-a`, Node Pool: `substrate-node-pool` with 2 x `c3-standard-4` nodes)
- **GCS Snapshot Bucket**: `gs://ax-instance5-snap-77658989016/`
- **GCP Service Account / IAM**: `ax-instance5-sa@barni-cnrm-20260529.iam.gserviceaccount.com`
- **Container Repositories & Images**:
  - Substrate Control Plane: `gcr.io/barni-cnrm-20260529/ax-instance5/*`
  - AX Task Runner: `gcr.io/barni-cnrm-20260529/ax-instance5/ax-task-runner:latest`
  - AX Platform Images: `gcr.io/barni-cnrm-20260529/ax-instance5/*`
- **Kubernetes Workloads & Namespaces**:
  - Namespace `ate-system`: Agent Substrate control plane (`atelet` DaemonSet, `ate-api-server`, `substrate-controller`, `atenet-router`, CRDs)
  - Namespace `ax-system`: AX platform components (`ax-redis` StatefulSet/PVC, `ax-controller` Deployment, `ax-server` Deployment)

## Estimated Cost
- Approximately **$0.40 - $0.50 per hour** of runtime (two `c3-standard-4` VMs, standard boot disks, GKE cluster management fee if applicable, Redis persistent disk, and minimal GCS storage).
- Complete cleanup via `teardown.sh` returns ongoing cost to $0.

## Feasibility Checklist
- [x] **Permissions**:
  - GKE Cluster Provisioning (`container.clusters.*`, `container.nodePools.*`)
  - Snapshot GCS Bucket Management (`storage.buckets.*`)
  - Project IAM Bindings (`iam.serviceAccounts.*`, `resourcemanager.projects.setIamPolicy`)
- [x] **gcloud CLI**: ✓ (version 585.0.0, present and authenticated with `cnrm-barni-1.svc.id.goog`)
- [x] **kubectl CLI**: ✓ (version 1.35.8, present)
- [x] **jq**: ✓ (present)
- [x] **Go Compiler**: ✓ (version 1.27.1, present)
- [ ] **ko CLI**: ✗ MISSING (will be installed automatically during `deploy.sh` via `go install github.com/google/ko@latest`)
- [ ] **docker/podman daemon**: ✗ MISSING (not required; `ax-task-runner` binary is compiled locally with Go and built via Google Cloud Build; `ko` is daemonless)

## Important Notes in deploy.sh
- All cloud resources and container images use the unique prefix `ax-instance5` to ensure complete tenant isolation and ownership.
- GKE node pool auto-upgrade is disabled on `substrate-node-pool` to prevent disruption during execution.
- Workload Identity and IAM bindings configure access for both Substrate and AX components to `gs://ax-instance5-snap-77658989016/`.
